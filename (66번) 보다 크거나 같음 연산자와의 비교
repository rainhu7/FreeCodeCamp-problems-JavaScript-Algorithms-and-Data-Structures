보다 크거나 같음 연산자와의 비교
크거나 같음 연산자( >=)는 두 숫자의 값을 비교합니다. 왼쪽의 숫자가 오른쪽의 숫자보다 크거나 같으면 를 반환합니다 true. 그렇지 않으면 를 반환합니다 false.

같음 연산자와 마찬가지로 크거나 같음 연산자는 비교하는 동안 데이터 유형을 변환합니다.

예

6   >=  6  // true
7   >= '3' // true
2   >=  3  // false
'7' >=  9  // false

문제
반환 문이 의미가 있도록 지정된 줄에 보다 크거나 같음 연산자를 추가합니다.

테스트
합격 :testGreaterOrEqual(0)문자열을 반환해야 합니다Less than 10
실패 :testGreaterOrEqual(9)문자열을 반환해야 합니다Less than 10
실패 :testGreaterOrEqual(10)문자열을 반환해야 합니다10 or Over
실패 :testGreaterOrEqual(11)문자열을 반환해야 합니다10 or Over
실패 :testGreaterOrEqual(19)문자열을 반환해야 합니다10 or Over
합격 :testGreaterOrEqual(100)문자열을 반환해야 합니다20 or Over
합격 :testGreaterOrEqual(21)문자열을 반환해야 합니다20 or Over
실패 :>=연산자를 두 번 이상 사용해야 합니다.

function testGreaterOrEqual(val) {
  if (val) {  // Change this line
    return "20 or Over";
  }

  if (val) {  // Change this line
    return "10 or Over";
  }

  return "Less than 10";
}

testGreaterOrEqual(10);

답

function testGreaterOrEqual(val) {
  if (val >=  20) {  // Change this line
    return "20 or Over";
  }

  if (val >= 10) {  // Change this line
    return "10 or Over";
  }

  return "Less than 10";
}

testGreaterOrEqual(10);

