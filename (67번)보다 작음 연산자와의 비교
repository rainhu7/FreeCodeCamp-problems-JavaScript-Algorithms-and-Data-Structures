보다 작음 연산자와의 비교
보다 작음 연산자( <)는 두 숫자의 값을 비교합니다. 왼쪽 숫자가 오른쪽 숫자보다 작으면 을 반환합니다 true. 그렇지 않으면 를 반환합니다 false. 같음 연산자와 마찬가지로 보다 작음 연산자는 비교하는 동안 데이터 유형을 변환합니다.

예

2   < 5 // true
'3' < 7 // true
5   < 5 // false
3   < 2 // false
'8' < 4 // false

문제
반환 문이 이해되도록 표시된 줄에 보다 작음 연산자를 추가합니다.

테스트
대기중 :testLessThan(0)문자열을 반환해야 합니다Under 25
대기중 :testLessThan(24)문자열을 반환해야 합니다Under 25
대기중 :testLessThan(25)문자열을 반환해야 합니다Under 55
대기중 :testLessThan(54)문자열을 반환해야 합니다Under 55
대기중 :testLessThan(55)문자열을 반환해야 합니다55 or Over
대기중 :testLessThan(99)문자열을 반환해야 합니다55 or Over
대기중 :<연산자를 두 번 이상 사용해야 합니다.

function testLessThan(val) {
  if (val) {  // Change this line
    return "Under 25";
  }

  if (val) {  // Change this line
    return "Under 55";
  }

  return "55 or Over";
}

testLessThan(10);


답
function testLessThan(val) {
  if (val < 25) {  // Change this line
    return "Under 25";
  }

  if (val < 55) {  // Change this line
    return "Under 55";
  }

  return "55 or Over";
}

testLessThan(10);
