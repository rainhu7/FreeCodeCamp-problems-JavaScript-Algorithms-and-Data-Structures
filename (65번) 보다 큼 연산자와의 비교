보다 큼 연산자와의 비교
보다 큼 연산자( >)는 두 숫자의 값을 비교합니다. 왼쪽의 숫자가 오른쪽의 숫자보다 크면 을 반환합니다 true. 그렇지 않으면 를 반환합니다 false.

같음 연산자와 마찬가지로 보다 큼 연산자는 비교하는 동안 값의 데이터 유형을 변환합니다.

예

5   >  3  // true
7   > '3' // true
2   >  3  // false
'1' >  9  // false
반환 문이 이해되도록 표시된 줄에 보다 큼 연산자를 추가합니다.

테스트
대기중 :testGreaterThan(0)문자열을 반환해야 합니다10 or Under
대기중 :testGreaterThan(10)문자열을 반환해야 합니다10 or Under
대기중 :testGreaterThan(11)문자열을 반환해야 합니다Over 10
대기중 :testGreaterThan(99)문자열을 반환해야 합니다Over 10
대기중 :testGreaterThan(100)문자열을 반환해야 합니다Over 10
대기중 :testGreaterThan(101)문자열을 반환해야 합니다Over 100
대기중 :testGreaterThan(150)문자열을 반환해야 합니다Over 100
대기중 :>연산자를 두 번 이상 사용해야 합니다.


문제
function testGreaterThan(val) {
  if (val) {  // Change this line
    return "Over 100";
  }

  if (val) {  // Change this line
    return "Over 10";
  }

  return "10 or Under";
}

testGreaterThan(10);

답
function testGreaterThan(val) {
  if (val > 100) {  // Change this line
    return "Over 100";
  }

  if (val > 10) {  // Change this line
    return "Over 10";
  }

  return "10 or Under";
}

testGreaterThan(10);
