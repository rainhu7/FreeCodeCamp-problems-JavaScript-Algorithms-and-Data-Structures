엄격한 부등식 연산자와의 비교
완전 부등식 연산자( !==)는 완전 항등 연산자의 논리적 반대입니다. 이것은 "Strictly Not Equal"을 의미 false하며 엄격한 평등이 반환되는 곳을 반환 true하고 그 반대 의 경우도 마찬가지 입니다. 엄격한 부등식 연산자는 데이터 유형을 변환하지 않습니다.

예

3 !==  3  // false
3 !== '3' // true
4 !==  3  // true
문 에 엄격한 부등식 연산자를 추가하여 가 다음 과 엄격하게 같지 않을 때 if함수가 문자열을 반환하도록 합니다 .Not Equalval17

테스트
대기중 :testStrictNotEqual(17)문자열을 반환해야 합니다Equal
대기중 :testStrictNotEqual("17")문자열을 반환해야 합니다Not Equal
대기중 :testStrictNotEqual(12)문자열을 반환해야 합니다Not Equal
대기중 :testStrictNotEqual("bob")문자열을 반환해야 합니다Not Equal
대기중 :!==연산자 를 사용해야 합니다.

문제
// Setup
function testStrictNotEqual(val) {
  if (val) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

testStrictNotEqual(10);

답
// Setup
function testStrictNotEqual(val) {
  if (val !== 17) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

testStrictNotEqual(10);
