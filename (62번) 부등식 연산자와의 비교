부등식 연산자와의 비교
부등식 연산자( !=)는 등식 연산자의 반대입니다. 같지 않음을 의미하고 false평등이 반환되는 곳을 반환 true하고 그 반대 의 경우도 마찬가지 입니다. 항등 연산자와 마찬가지로 부등 연산자는 비교하는 동안 값의 데이터 유형을 변환합니다.

예

1 !=  2    // true
1 != "1"   // false
1 != '1'   // false
1 != true  // false
0 != false // false
가 와 같지 않을 때 함수가 문자열을 반환하도록 명령문 에 부등식 연산자 !=를 추가합니다 .ifNot Equalval99

테스트
대기중 :testNotEqual(99)문자열을 반환해야 합니다Equal
대기중 :testNotEqual("99")문자열을 반환해야 합니다Equal
대기중 :testNotEqual(12)문자열을 반환해야 합니다Not Equal
대기중 :testNotEqual("12")문자열을 반환해야 합니다Not Equal
대기중 :testNotEqual("bob")문자열을 반환해야 합니다Not Equal
대기중 :!=연산자 를 사용해야 합니다.

문제 
// Setup
function testNotEqual(val) {
  if (val) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

testNotEqual(10);

답 
// Setup
function testNotEqual(val) {
  if (val !=  99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

testNotEqual(10);
